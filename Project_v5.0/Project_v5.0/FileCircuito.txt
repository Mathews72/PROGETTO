module Circuito (
		clk
		input a, b, e, pippo
		input c, var
		input d

		output x, y
);
	
    FF1 = a AND c
	FF2 = FF1 AND c
	assign y = b AND FF2 OR var
	assign z = e AND d

	
endmodule 

module Circuito2 (
		clk
		input a, b, e, pippo
		input c, var
		input d

		output x, y, k, z
);
	
    FF1 = a AND c
	
	assign y = b XOR FF1
	assign z = e OR d
	assign k = FF1 NAND b

	
endmodule 




