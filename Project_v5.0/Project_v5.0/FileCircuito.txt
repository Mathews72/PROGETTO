module Circuito (
		clk
		input a, b
		input d, e[2], pippo
		input c, var

		output x, y
);

FF1 = a NAND b
FF2 = FF1 XOR d 
assign x = e[0] AND FF2


 endmodule 


module Circuito2 (
		clk
		input a, b, e, pippo
		input c, var
		input d

		output x, y, k, z
);
	
    FF1 = a AND c
	
	assign y = b XOR FF1
	assign z = e OR d
	assign k = FF1 NAND b

	
endmodule 




